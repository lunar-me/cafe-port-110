# Development dependencies: everything needed to run the test suite.
#
# Install with:  pip install -r requirements-dev.txt
#
# The tests start the server in a subprocess and speak POP3, SMTP and smtplib
# over real sockets, so trio has to be present as well -- it comes in through
# the runtime file below rather than being repeated here.
-r requirements.txt

# Optional: the same tests also run as "python test_cafeport110.py", with no
# pytest installed at all. 7.0 is the last release supporting Python 3.8,
# matching the requires-python = ">=3.8" in pyproject.toml.
pytest>=7.0
